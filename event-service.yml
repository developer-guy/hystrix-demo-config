server:
  port: 0
  
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
ribbon:
  ConnectTimeout:  5000
  ReadTimeout:  5000

api:
  root: https://api.meetup.com
  token: 221a6adea7a10f1d293a97ce02eeee95
  events-rate-limit: 2
  event-rate-limit: 2

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
