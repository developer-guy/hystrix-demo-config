spring:
  cache:
   ehcache:
    config: classpath:ehcache.xml

server:
  port: 0
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
ribbon:
  ConnectTimeout:  10000
  ReadTimeout:  10000

api:
  root: https://api.meetup.com
  token: b4c78dc4031f2608ca9439cd13dac92f
  rate-limit: 4

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
